# NetLogo Lexer for Pygments

This is a NetLogo lexer for [Pygments](https://pygments.org/), which is used by the LaTeX package [minted](https://github.com/gpoore/minted) for syntax highlighting. To install, run `pip install .` in the source directory.
